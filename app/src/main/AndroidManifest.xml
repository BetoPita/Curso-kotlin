<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.albertopita.holamundo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LifeCycleActivity" />
        <activity android:name=".activities.ClickEventsActivity" />
        <activity android:name=".activities.LifeCycle2Activity" />
        <activity android:name=".activities.TestActivity" />
        <activity android:name=".kotlinAndroidExtensionActivity" />
        <activity android:name=".PicassoActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".IntentsActivity" />
        <activity android:name=".PermissionsActivity" />
        <activity android:name=".SharedPreferencesActivity" />
        <activity android:name=".ExtensionFunctionsActivity" />
        <activity android:name=".LifeCycleActivity" />
        <activity android:name=".ClickEventsActivity" />
        <activity android:name=".activities.IntentExtrasActivity"></activity>
    </application>

</manifest>